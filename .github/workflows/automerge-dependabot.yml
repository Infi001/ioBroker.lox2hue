# Automatically merge Dependabot PRs when version comparison is within the range
# that is configured in .github/auto-merge.yml
#
# Requires a repository secret AUTO_MERGE_TOKEN: a Personal Access Token with
# "public_repo" scope, created at https://github.com/settings/tokens. Without
# it this workflow does nothing (dependabot PRs still need manual review/merge).

name: Auto-Merge Dependabot PRs

on:
  # WARNING: This needs to be run in the PR base, DO NOT build untrusted code in this action
  # details under https://github.blog/changelog/2021-02-19-github-actions-workflows-triggered-by-dependabot-prs-will-run-with-read-only-permissions/
  pull_request_target:

jobs:
  auto-merge:
    if: github.actor == 'dependabot[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Check if PR should be auto-merged
        uses: iobroker-bot-orga/action-automerge-dependabot@v1
        with:
          github-token: ${{ secrets.AUTO_MERGE_TOKEN }}
          # Squash and merge, so Github chooses nice commit messages
          merge-method: squash
